<%= form_for(@product_ingredient) do |f| %>
  <% if @product_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_ingredient.errors.count, "error") %> prohibited this product_ingredient from being saved:</h2>

      <ul>
      <% @product_ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.text_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :ingredient_id %><br>
    <%= f.text_field :ingredient_id %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
